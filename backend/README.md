## Backend env qayerda?

API key qo‘yish uchun:

1) `backend/.env.example` faylni `backend/.env` qilib nusxalang
2) `backend/.env` ichiga secret’larni yozing

Masalan:

```env
GOOGLE_STT_API_KEY=AIzaSy...
PORT=3001
```

⚠️ `backend/.env` ni git’ga commit qilmang.

